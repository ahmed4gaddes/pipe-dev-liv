<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.2</version> <!-- Version corrigée pour la compatibilité Spring Cloud -->
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    
    <groupId>com.pipedevliv</groupId>
    <artifactId>pipe-dev-liv-backend</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <packaging>pom</packaging>
    
    <name>pipe-dev-liv-backend</name>
    <description>Projet Parent pour les microservices Pipe Dev Liv</description>

    <modules>
        <module>common-lib</module>
        <module>discovery-server</module>
        <module>api-gateway</module>
        <module>user-service</module>
        <module>ticket-service</module>
        <module>pipeline-service</module>
        <module>notification-service</module>
        <module>audit-service</module>
    </modules>

    <properties>
        <java.version>17</java.version>
        <spring-cloud.version>2023.0.3</spring-cloud.version>
        <!-- Testcontainers 1.19.8 (géré par le BOM Spring Boot 3.3.2) embarque docker-java 3.3.6,
             qui envoie des requêtes API pinnées sur une ancienne version que Docker Desktop rejette
             désormais (HTTP 400 sur /v1.24, /v1.32 ...). On force une version plus récente. -->
        <testcontainers.version>1.21.4</testcontainers.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Force le répertoire de travail (racine du repo) pour "mvnw ... spring-boot:run",
                     pour qu'il corresponde à celui utilisé par IntelliJ (constaté : $PROJECT_DIR$),
                     et que spring-dotenv (voir dépendance ci-dessous) trouve le .env au même endroit
                     quelle que soit la façon dont le service est lancé. -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <workingDirectory>${maven.multiModuleProjectDirectory}/..</workingDirectory>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        <plugins>
            <!-- Déclaré ici (pas pluginManagement) pour être hérité par les 8 modules sans
                 qu'aucun n'ait à le redéclarer. Produit target/site/jacoco/jacoco.xml, consommé par
                 l'analyse SonarQube (Phase 9, ci.yml) pour la couverture de test. Les exécutions
                 "*-integration" (Phase 10) font la même chose pour les tests Failsafe (*IT.java,
                 target/site/jacoco-it/jacoco.xml) — agent séparé (failsafeArgLine) pour ne pas
                 mélanger la couverture unitaire et la couverture d'intégration. -->
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.12</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>test</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>prepare-agent-integration</id>
                        <phase>pre-integration-test</phase>
                        <goals>
                            <goal>prepare-agent-integration</goal>
                        </goals>
                        <configuration>
                            <propertyName>failsafeArgLine</propertyName>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report-integration</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report-integration</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <!-- Tests d'intégration Testcontainers (*IT.java, Phase 10) — séparés des tests unitaires
                 Surefire (*Test.java, H2/mocks) : plus lents (vraie Postgres/RabbitMQ, Docker requis),
                 exécutés à la phase integration-test/verify plutôt que test. `mvn verify` (déjà utilisé
                 partout, y compris ci.yml) les déclenche automatiquement — aucun changement CI requis. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <argLine>${failsafeArgLine}</argLine>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <!-- Dépendances communes à tous les modules -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <!-- Charge le .env de la racine du repo dans l'Environment Spring Boot au démarrage.
             Chaque module doit fournir un .env.properties (src/main/resources) avec
             directory=../../ pour pointer vers la racine depuis backend/<module>/. -->
        <dependency>
            <groupId>me.paulschwarz</groupId>
            <artifactId>spring-dotenv</artifactId>
            <version>4.0.0</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Tests d'intégration (Phase 10) — version gérée par le BOM testcontainers-bom importé
             par spring-boot-dependencies (hérité via spring-boot-starter-parent), pas besoin de la
             fixer ici. -->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>postgresql</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>rabbitmq</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- Attente asynchrone (Phase 10) — nécessaire pour les IT qui publient un message et
             attendent qu'un @RabbitListener le consomme et persiste en base sur un autre thread. -->
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>4.2.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

</project>
